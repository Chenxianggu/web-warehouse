DATABASE_URL="postgresql://warehouse:warehouse@localhost:5433/warehouse?schema=public"
AUTH_SECRET="replace-with-a-random-secret-at-least-32-characters-long"
AUTH_URL="http://localhost:3000"
# ADMIN_INITIAL_PASSWORD is optional for the first seed. If omitted, a random password is printed once.
# ADMIN_INITIAL_PASSWORD=""

# Production deployment only. The release tag is injected by GitHub Actions.
# IMAGE_REPOSITORY="warehouse-app"
# IMAGE_TAG="v1.0.0"
